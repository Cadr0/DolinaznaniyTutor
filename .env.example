# Локальная разработка
DATABASE_URL="postgresql://dolinaznaniy:dolinaznaniy@localhost:5432/dolinaznaniy?schema=public"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"
AUTH_SECRET="change-me-local-dev-secret-min-32-chars"

# Локальное хранилище изображений заданий (по умолчанию ./uploads)
# UPLOADS_DIR="./uploads"

# Импорт из старой БД (scripts/import-legacy-tasks.ts)
# IMPORT_TUTOR_EMAIL="teacher@example.com"
# LEGACY_SQL_PATH="./old bd/u188809_dolinaznaniy (1).sql"
# LEGACY_UPLOADS_DIR="./old bd/uploads/tasks"

# Домен (prod)
# NEXT_PUBLIC_APP_URL="https://diary-ai.ru"

# SMTP smtp.bz (prod — см. CONNECTION.local.md)
# SMTP_HOST=connect.smtp.bz
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@diary-ai.ru
