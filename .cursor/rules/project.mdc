---
description: Core project context for Dolinaznaniy Tutor — read AGENTS.md first
alwaysApply: true
---

# Dolinaznaniy Tutor

**Entry point:** `AGENTS.md` (main file for any AI agent). Secrets: `CONNECTION.local.md`.

Product: tutoring platform. Repo: Cadr0/DolinaznaniyTutor. Prod: http://111.88.118.35

**Before coding:** read `docs/ROADMAP.md` (current phase: Stage 2 — Auth).

**Structure:** `src/app/` pages+API · `src/lib/` shared · `prisma/schema.prisma` DB · `infra/` deploy only · `docs/` all docs.

**Prefer proven solutions:** official docs, established npm packages, MCP tools (Context7, Supabase, Vercel). Do not reinvent auth/UI/infra.

**Verify truth:** never invent APIs, env vars, CLI flags, or claim success without running checks (build, curl, SSH, logs). Read docs or code first.

**Do not:** separate backend, secrets in git, files in obsolete `scripts/` or `site/`, duplicate docs in root.

**Deploy:** push `main` → GitHub Actions. Ops: `.\infra\remote-ops.ps1 status` (needs `$env:VDS_PASSWORD`).
