# prisma — модель данных

Единственный источник схемы БД: `schema.prisma`.

```bash
npm run db:push    # dev: синхрон с локальной БД
npm run db:studio  # GUI
```

Сущности: User, Room, RoomMember, Assignment, Submission, MarketplaceTask — см. `docs/PRODUCT.md`.
